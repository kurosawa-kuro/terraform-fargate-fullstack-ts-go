name: CI

on:
  push:
    branches:
      - production
      - development
  pull_request:
    branches:
      - production
      - development

jobs:
  basic-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Project validation
        run: |
          echo "✅ リポジトリの基本チェックを実行中..."
          echo "✅ チェック完了"
