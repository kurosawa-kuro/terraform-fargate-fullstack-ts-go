name: CD

on:
  push:
    branches:
      - production
      - development
  pull_request:
    branches:
      - production
      - development

jobs:
  deployment-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deployment validation
        run: |
          echo "✅ デプロイの基本チェックを実行中..."
          echo "✅ デプロイチェック完了"